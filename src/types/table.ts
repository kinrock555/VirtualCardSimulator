// Re-exported from shared/ so the client and the online server (server/)
// use the exact same card/table data model without duplicating it.
export type { CardRotation, CardZone, CardInstance, StackType, CardStack } from '../../shared/cardTypes';
